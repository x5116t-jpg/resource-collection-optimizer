# 配布パッケージ手動作成手順

プロジェクト全体を配布するための手順です。

## 📦 簡単な方法：手動コピー

### ステップ1: 配布フォルダを作成

任意の場所に以下のフォルダ構造を作成：

```
ResouceCollection_05_Distribution/
├── project/
└── superclaude/
```

### ステップ2: プロジェクトファイルをコピー

#### コピーするもの：
```
D:\py\Resource Collection\ResouceCollection_05\
```

から以下を `project/` フォルダにコピー：

✅ **コピーするフォルダ:**
- src/
- scripts/
- tests/
- data/
- docs/
- claudedocs/
- networkx/
- .claude/

✅ **コピーするファイル:**
- *.bat (全ての.batファイル)
- requirements.txt

❌ **コピーしないフォルダ:**
- .venv/
- .pytest_cache/
- SuperClaude_Setup/
- __pycache__/

### ステップ3: SuperClaude設定をコピー

```
D:\py\Resource Collection\ResouceCollection_05\SuperClaude_Setup\
```

の全内容を `superclaude/` フォルダにコピー

### ステップ4: セットアップスクリプトを追加

`ResouceCollection_05_Distribution/` のルートに以下のファイルを作成：

#### setup.bat
```batch
@echo off
echo ResouceCollection_05 セットアップ
echo.
echo [1/2] SuperClaude設定をインストール中...
cd superclaude
if exist "Windows用インストール.bat" (
    call "Windows用インストール.bat"
) else (
    echo SuperClaude設定が見つかりません
)
cd ..

echo.
echo [2/2] Python仮想環境を作成中...
cd project
echo Python仮想環境を作成しますか？
echo （既にある場合はスキップしてください）
pause

python -m venv .venv
call .venv\Scripts\activate.bat
pip install -r requirements.txt

echo.
echo セットアップ完了！
echo.
echo 次のステップ:
echo 1. Claude Code を起動
echo 2. project フォルダを開く
pause
```

#### README.txt
```
ResouceCollection_05 配布パッケージ

【インストール方法】

Windows:
  1. setup.bat をダブルクリック
  2. 画面の指示に従う

Mac:
  1. superclaude/ フォルダ内の「Mac用インストール.command」を実行
  2. project/ フォルダで仮想環境を作成:
     cd project
     python3 -m venv .venv
     source .venv/bin/activate
     pip install -r requirements.txt

【使い方】

Claude Code を起動して project フォルダを開いてください。
SuperClaude機能が自動的に有効になります。

詳細は superclaude/docs/ フォルダ内のドキュメントを参照してください。
```

---

## 🚀 PowerShellを使った自動コピー（推奨）

以下のPowerShellコマンドで一括コピー：

```powershell
# 変数設定
$source = "D:\py\Resource Collection\ResouceCollection_05"
$dest = "D:\py\Resource Collection\ResouceCollection_05_Distribution"

# フォルダ作成
New-Item -ItemType Directory -Path "$dest\project" -Force
New-Item -ItemType Directory -Path "$dest\superclaude" -Force

# プロジェクトファイルをコピー（除外設定）
$exclude = @('.venv', '.pytest_cache', 'SuperClaude_Setup', '__pycache__')

Get-ChildItem $source | Where-Object {
    $_.Name -notin $exclude
} | Copy-Item -Destination "$dest\project\" -Recurse -Force -Exclude "*.pyc"

# SuperClaude設定をコピー
Copy-Item "$source\SuperClaude_Setup\*" -Destination "$dest\superclaude\" -Recurse -Force

Write-Host "コピー完了！"
```

---

## ✅ 完成後の確認

配布フォルダが以下の構成になっているか確認：

```
ResouceCollection_05_Distribution/
│
├── setup.bat                (セットアップスクリプト)
├── README.txt               (説明ファイル)
│
├── project/                 (プロジェクト本体)
│   ├── src/
│   ├── scripts/
│   ├── tests/
│   ├── data/
│   ├── docs/
│   ├── claudedocs/
│   ├── networkx/
│   ├── .claude/
│   ├── *.bat
│   └── requirements.txt
│
└── superclaude/             (SuperClaude設定)
    ├── Windows用インストール.bat
    ├── Mac用インストール.command
    ├── README.txt
    ├── config_files/
    └── docs/
```

---

## 📦 ZIP圧縮

配布準備が完了したら：

1. ResouceCollection_05_Distribution フォルダ全体を右クリック
2. 「送る」→「圧縮（zip形式）フォルダー」
3. ファイル名を `ResouceCollection_05_v1.0.zip` に変更

または PowerShell で：

```powershell
Compress-Archive -Path "D:\py\Resource Collection\ResouceCollection_05_Distribution" `
                 -DestinationPath "D:\py\Resource Collection\ResouceCollection_05_v1.0.zip"
```

---

## 🎯 配布時のチェックリスト

- [ ] プロジェクトファイルが全てコピーされている
- [ ] .venv フォルダが除外されている
- [ ] SuperClaude設定が含まれている
- [ ] setup.bat が動作する
- [ ] README.txt が分かりやすい
- [ ] 機密情報が含まれていない
- [ ] requirements.txt が正しい
- [ ] 別環境でテストして動作確認

完了したらZIP化して配布可能です！
